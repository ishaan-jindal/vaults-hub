[project]
name = "vaults-hub"
version = "0.1.0"
description = "Stdio MCP server for Obsidian-style Markdown vaults"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "Ishaan Jindal" }]
dependencies = [
  "anyio==4.9.0",
  "mcp==1.30.0",
  "PyYAML==6.0.3",
]

[tool.ruff]
line-length = 100
